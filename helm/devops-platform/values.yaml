replicaCount: 2

image:
  repository: ghcr.io/infobeans-vishalmene/devops-platform
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8000

containerPort: 8000

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

labels:
  app: devops-platform